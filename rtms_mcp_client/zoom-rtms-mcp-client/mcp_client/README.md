# RTMS MCP Client

Receives authenticated Zoom RTMS webhooks, subscribes only to transcript media, batches transcript text per stream, and calls the authenticated internal LLM router.

See the project-level `README.md` for Zoom Marketplace, security, configuration, and Docker instructions. This service defaults to port `3000`.
